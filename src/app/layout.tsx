import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Laura Tagliari | Mentoria com apresentações",
  description:
    "Portfólio de Laura Tagliari, designer especialista em mentoria com apresentações profissionais.",
  openGraph: {
    title: "Laura Tagliari | Mentoria com apresentações",
    description:
      "Slides, narrativas visuais e apresentações profissionais criadas para comunicar ideias com clareza.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-white">{children}</body>
    </html>
  );
}
